# Session artifacts — deny-by-default, never commit
.agents/
.claude/
# macOS
.DS_Store

# Obsidian workspace cache (keep config, ignore volatile state)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
